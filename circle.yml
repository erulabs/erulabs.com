machine:
  pre:
    - mkdir ~/.yarn-cache
  node:
    version: v7.5.0

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 0.20.3
  cache_directories:
    - ~/.yarn-cache
  override:
    - yarn install

test:
  override:
    - ./bin/lint.sh
