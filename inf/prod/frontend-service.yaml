apiVersion: v1
kind: Service
metadata:
  name: erulabscom-frontend
spec:
  type: ClusterIP
  ports:
    - name: tcp
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: erulabscom
    tier: frontend

